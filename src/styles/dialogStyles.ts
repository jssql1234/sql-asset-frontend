import { setDialogTheme } from "@/components/ui/components";

setDialogTheme({
  dialogClose: {
    variant: "dialog",
  },
  dialogTitle: {
    base: "flex flex-wrap title-small leading-none text-onSurface w-full text-start",
    className: "",
  }
});